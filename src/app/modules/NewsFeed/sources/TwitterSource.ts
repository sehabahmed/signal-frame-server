import config from "../../../config";
import { INewsSource } from "../newsfeed.interface";

export class TwitterSource implements INewsSource {
    private readonly bearerToken = config
    private readonly url =
}